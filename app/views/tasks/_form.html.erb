<%= form_for @task do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <% if @task.new_record?() == false %>
    <%= f.check_box :completed %>
    <%= f.label :completed %>
  <% end %>
  <%= f.submit %>
<% end %>
